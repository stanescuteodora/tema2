#!/bin/bash
chmod 755 powsnsums.sh
for arg in "$@"
do 
	echo "$arg"
done
if ($arg==^[0-9]+$

echo "Numarul de argumente este $#"
for ((i=$0;i<=$n;i++))
do
       	sum=$((&sum + $i))
done
	echo "$sum"
	
